{
  "name": "xcore-framework",
  "version": "0.1.0a1",
  "scripts": {
    "release": "standard-version",
    "postrelease": "node scripts/update-setup-version.js",
    "release:patch": "standard-version --release-as patch --skip.commit --skip.tag",
    "release:minor": "standard-version --release-as minor --skip.commit --skip.tag",
    "release:major": "standard-version --release-as major --skip.commit --skip.tag"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  },
  "dependencies": {
    "axios": "^1.9.0"
  }
}
